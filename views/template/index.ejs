<article>
	<header>
		<h1>clover.blue</h1>
	</header>

	<div class="content">
		<figure class="main" style="background-image: url(http://farm{{flickr.items[0].farm}}.staticflickr.com/{{flickr.items[0].server}}/{{flickr.items[0].id}}_{{flickr.items[0].secret}}_b.jpg);"><img ng-src="http://farm{{flickr.items[0].farm}}.staticflickr.com/{{flickr.items[0].server}}/{{flickr.items[0].id}}_{{flickr.items[0].secret}}_b.jpg"></figure>

		<!--<section>-->
			<!--<h2 class="off"><a href="design">Design</a> &amp; <a href="photo">Photo</a></h2>-->
			<!--<ul class="thumbnails center max" ng-class="{loading: flickr.showLoading}">-->
				<!--<li ng-repeat="item in tumblrDesign.items | limitTo:5">-->
					<!--<a ng-href="{{item.original_size.url}}" ga="'send', 'event', '{{currentPage}}', 'click', '{{item.id}}_{{$index}}'" title="{{item.caption | tumblrDomainReplace}}" colorbox="{transition:'none', maxWidth: '90%'}" rel="items"><img ng-src="{{item.alt_sizes[item.alt_sizes.length - 1].url}}"></a>-->
				<!--</li>-->
				<!--<li ng-repeat="item in flickr.items | limitTo:5" ng-cloak>-->
					<!--<a href="http://farm{{item.farm}}.staticflickr.com/{{item.server}}/{{item.id}}_{{item.secret}}_b.jpg" ga="'send', 'event', '{{currentPage}}', 'click', '{{item.id}}'" colorbox="{transition:'none', maxWidth: '90%'}"><img ng-src="http://farm{{item.farm}}.staticflickr.com/{{item.server}}/{{item.id}}_{{item.secret}}_q.jpg"></a>-->
				<!--</li>-->
			<!--</ul>-->
			<!--<p ng-cloak ng-show="flickr.showErrorMessage" class="center"><small>apiのメンテナンス、もしくは読み込み環境の影響で読み込みに失敗しました。<br>時間をおいてお試しください。</small></p>-->
		<!--</section>-->

		<section>
			<h2><a href="weblog">Weblog</a></h2>
			<ul class="tags">
				<li ng-repeat="tag in qiita.tags" ng-cloak>
					<a ng-href="/weblog/tags/{{tag}}" ga="'send', 'event', '{{currentPage}}', 'click', '{{tag}}'">{{tag}}</a>
				</li>
			</ul>
			<ul ng-class="{loading: qiita.showLoading}">
				<li ng-repeat="item in qiita.items | limitTo:5" ng-cloak>
					<a ng-href="/items/{{item.uuid}}" ga="'send', 'event', '/', 'click', '{{item.title}}'">{{item.title}}</a>
				</li>
			</ul>
			<p ng-cloak ng-show="qiita.showErrorMessage" class="center"><small>apiのメンテナンス、もしくは読み込み環境の影響で読み込みに失敗しました。<br>時間をおいてお試しください。</small></p>
		</section>

		<section>
			<h2><a href="diary">Diary</a></h2>
			<ul class="tags">
				<li ng-repeat="tag in tumblr.tags" ng-cloak>
					<a ng-href="/diary/tags/{{tag}}" ga="'send', 'event', '{{currentPage}}', 'click', '{{tag}}'">{{tag}}</a>
				</li>
			</ul>
			<ul ng-class="{loading: tumblr.showLoading}">
				<li ng-repeat="item in tumblr.items" ng-cloak ng-if="item.type !== 'photo'">
					<a ng-href="/post/{{item.id}}" ga="'send', 'event', '{{currentPage}}', 'click', '{{item.title}}'">{{item.title}}</a>
				</li>
			</ul>
			<p ng-cloak ng-show="tumblr.showErrorMessage" class="center"><small>apiのメンテナンス、もしくは読み込み環境の影響で読み込みに失敗しました。<br>時間をおいてお試しください。</small></p>
		</section>

		<!--<section>-->
			<!--<h2>最新イラスト</h2>-->
			<!--<ul class="thumbnails center" ng-class="{loading: pixiv.showLoading}">-->
				<!--<li ng-repeat="item in pixiv.items | limitTo:8" ng-cloak>-->
					<!--<a href="{{item.img}}" ga="'send', 'event', '{{currentPage}}', 'click', '{{item.title}}'" colorbox="{transition:'none', maxWidth: '90%'}" title="{{item.title}}"><img ng-src="{{item.thumbnail}}"></a>-->
				<!--</li>-->
			<!--</ul>-->
			<!--<p ng-cloak ng-show="pixiv.showErrorMessage" class="center"><small>apiのメンテナンス、もしくは読み込み環境の影響で読み込みに失敗しました。<br>時間をおいてお試しください。</small></p>-->
		<!--</section>-->

		<sns></sns>
	</div>
</article>